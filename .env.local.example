# Webhook the contact modal's requests are forwarded to (see src/app/api/contact/route.ts).
# Copy this file to .env.local for local dev, and set CONTACT_WEBHOOK_URL as a
# Cloudflare Worker secret/variable for production.
CONTACT_WEBHOOK_URL=
